{
  "name": "htmx-hxboost",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-unfinished-node-thing": "nodemon -w src -e js,jsx --exec \"npx node esbuild.js\"",
    "go": "go run ./main.go",
    "start": "nodemon -w src -e js,jsx --exec \"node esbuild.js\"",
    "start2": "esbuild-dev-server --entry=src/index.jsx --bundle --outdir=public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.1",
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}